apiVersion: v1
kind: Service
metadata:
  name: model-serving
  namespace: ml-training
spec:
  type: ClusterIP
  selector:
    app: model-serving
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
